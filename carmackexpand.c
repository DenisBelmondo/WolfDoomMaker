#include <stdio.h>
#include <stdlib.h>

#include "carmackexpand.h"

u_int8_t* carmackExpand(u_int8_t* data, u_int16_t size,
						u_int16_t deSize)
{
	u_int8_t* deData = (u_int8_t *)malloc(deSize);
	
	if (deData != NULL)
	{
		int i;
		while(i < size)
		{
			
		}
	}
	else
	{
		return NULL;
	}
}
